<?xml version="1.0" encoding="utf-8"?>
<!-- canRetrieveWindowContent: inspect PIN field nodes for text length polling.
     canPerformGestures: dispatch swipe-up to dismiss lock screen overlay.
     canRequestFilterKeyEvents: powers the global search-overlay key trigger (see
       LockScreenAccessibilityService's onKeyEvent) — blocked on the secure keyguard itself, so no
       effect there, but active everywhere else.
     flagRetrieveInteractiveWindows: required for getWindows() to return TYPE_KEYGUARD window. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeAnnouncement"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows"
    android:canRequestFilterKeyEvents="true"
    android:canRetrieveWindowContent="true"
    android:description="@string/lock_accessibility_service_description"
    android:notificationTimeout="500"
    android:settingsActivity="com.zeno.classiclauncher.nlauncher.MainActivity" />
